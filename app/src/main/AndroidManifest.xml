<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- Notification permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Commande"
        tools:targetApi="31">

        <!-- MainActivity declared here -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare ListCommandesActivity here -->
        <activity android:name=".ListCommandesActivity"
            android:exported="true" />
        <!-- AddCommandeActivity -->
        <activity android:name=".AddCommandeActivity"
            android:exported="true" />

        <!-- UpdateDeleteCommandeActivity -->
        <activity android:name=".UpdateDeleteCommandeActivity"
            android:exported="true" />

        <!-- notification -->
        <activity android:name=".NotificationActivity"
            android:exported="true" />



    </application>

</manifest>
